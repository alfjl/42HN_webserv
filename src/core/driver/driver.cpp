#include "driver.hpp"

namespace webserv {
    namespace core {

        driver::driver(webserv& the_serv) : component(the_serv) {
            
        }

        driver::~driver() {

        }

    }
}
